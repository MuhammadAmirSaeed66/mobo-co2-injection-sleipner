# Simulator source

Repository:

```text
https://github.com/MuhammadAmirSaeed66/CO2InjectionModeling.jl
```

From the repository root, run:

```r
source("R/install_julia_simulator.R")
```

The installer records the downloaded commit in `SIMULATOR_COMMIT.txt`.
